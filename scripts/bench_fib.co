def fib(n: int) -> int {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

def main() {
    total = 0;
    for i in 0..=30 {
        total += fib(i);
    }
    print(total);
}