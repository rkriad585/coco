# stdlib/lib/math.co - numeric helpers (Coco-written, Phase 6).
# Built on the runtime `sqrt`/`abs`/`float`/`int` builtins. Import with:
#   import lib.math;

pub const PI: float = 3.1415926535897932384;
pub const E: float = 2.7182818284590452353;

# Square root via Newton's method (pure Coco, no native sqrt needed).
pub def sqrt(x: float) -> float {
    if x <= 0.0 { return 0.0; }
    g = x;
    i = 0;
    while i < 40 {
        g = (g + x / g) / 2.0;
        if g * g - x < 1e-12 and g * g - x > -1e-12 { break; }
        i = i + 1;
    }
    return g;
}

pub def abs(x) {
    if x < 0 { return -x; }
    return x;
}

# Clamp x into [lo, hi] (lo <= hi).
pub def clamp(x: float, lo: float, hi: float) -> float {
    if x < lo { return lo; }
    if x > hi { return hi; }
    return x;
}

# Smaller of two values (works for numbers; strings compare lexically).
pub def min2(a, b) {
    if a < b { return a; }
    return b;
}

# Larger of two values.
pub def max2(a, b) {
    if a > b { return a; }
    return b;
}

# Integer exponentiation: base^exp (exp >= 0).
pub def ipow(base: int, exp: int) -> int {
    r = 1;
    i = 0;
    while i < exp {
        r = r * base;
        i = i + 1;
    }
    return r;
}

# Floating power via repeated squaring (exp integral).
pub def fpow(base: float, exp: int) -> float {
    r = 1.0;
    i = 0;
    while i < exp {
        r = r * base;
        i = i + 1;
    }
    return r;
}