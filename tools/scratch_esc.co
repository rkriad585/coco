def show(s: string) {
    out = "";
    for i in range(len(s)) { out += str(ord(s[i])) + " "; }
    print(out);
}

def main() {
    show("a\tb");
    show("x\ny");
    show("\u{41}\u{22}");
    show("a\u{5c}b");
    print(str(67));
}