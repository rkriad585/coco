# expect: escape: '&x' must not be returned or stored
# 5.5 - passing the address of a local into a function escapes it (E-BORROW-ESCAPE).
def take(p: *int) {}
def main() {
    x = 42;
    take(&x);
}
