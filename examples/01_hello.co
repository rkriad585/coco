# 01 — Hello World                        (plan §5.0 · grammar §3)
# Expected someday: coco run examples/01_hello.co  →  Hello, World!
def main():
    name = "World"
    print(f"Hello, {name}!")

    lang = "Coco"
    print(f"{lang:>10} says hi")        # format spec inside interpolation
